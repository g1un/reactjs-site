import React from 'react';

export class Contacts extends React.Component {
    render() {
        return (
            <h1>
                {this.props.pageTitle}
            </h1>
        );
    }
}